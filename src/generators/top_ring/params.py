from __future__ import annotations

from dataclasses import dataclass


@dataclass
class TopRingParams:
    """Заглушка параметров верхнего кольца — будет реализовано позже."""

    enabled: bool = False
