from __future__ import annotations

from dataclasses import dataclass


@dataclass
class CylinderWallParams:
    """Заглушка параметров поверхности качения — будет реализовано позже."""

    enabled: bool = False
